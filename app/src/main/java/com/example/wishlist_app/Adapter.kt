class ItemAdapter (private val mItems: List<Wish>) : RecyclerView.Adapter {
    inner class ViewHolder(itemView: View) : RecyclerView.ViewHolder(itemView) {
        // Your holder should contain and initialize a member variable
    }
}
